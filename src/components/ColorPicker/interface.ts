export interface IPropsColorPicker {
    classes: Array<string>;
    id?: string;
    colorValue: string;
    refValue: any;
}

export interface ITypeColorSelector {
    classes: Array<string>;
    id?: string;
    colorValue: string;
    refValue: any;
}